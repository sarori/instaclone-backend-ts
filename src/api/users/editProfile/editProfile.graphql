type editProfileResponse {
	ok: Boolean!
	error: String
}

type Mutation {
	editProfile(
		id: Int!
		firstName: String
		lastName: String
		username: String
		email: String
		password: String
	): editProfileResponse!
}
